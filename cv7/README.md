

1. Vyzkoušejte implementaci funkce s proměnným počtem parametrů, která provede součet neznámého počtu čísel `int`, kdy poslední číslo bude `-1`. Otestujte pro 2, 4, 7 a 10 čísel.

2. Implementujte rekurzivně funkci `long mocnina( long t_num, long t_exp )`. Průběžně si vypisujte mezivýsledky.

3. Naimplementujte instrukci `bswap` bez `bswap`: `long bswap( long t_num )`. (pozor, zápis do 32-bitového registru nuluje horní čast).

4. Najděte první číslo v poli, které má všechny bajty stejné. `long hledej( long *t_array, int t_len )`;

